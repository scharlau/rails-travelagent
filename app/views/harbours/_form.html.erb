<%= form_with(model: harbour, local: true) do |form| %>
  <% if harbour.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(harbour.errors.count, "error") %> prohibited this harbour from being saved:</h2>

      <ul>
      <% harbour.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :country %>
    <%= form.text_field :country %>
  </div>

  <div class="field">
    <%= form.label :lat %>
    <%= form.text_field :lat %>
  </div>

  <div class="field">
    <%= form.label :long %>
    <%= form.text_field :long %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
